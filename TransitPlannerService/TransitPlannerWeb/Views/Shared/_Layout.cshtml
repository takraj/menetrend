<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>@ViewBag.Title - TUSZ</title>
        <link href="~/favicon.ico" rel="shortcut icon" type="image/x-icon" />
        <meta name="viewport" content="width=device-width" />

        <link href='http://fonts.googleapis.com/css?family=Play:400,700&subset=latin,latin-ext' rel='stylesheet' type='text/css' />
        <link href='http://fonts.googleapis.com/css?family=Dosis:400,700&subset=latin,latin-ext' rel='stylesheet' type='text/css' />

        @Styles.Render("~/css/jquery.css")
        @Styles.Render("~/css/fancytree.css")
        @Styles.Render("~/css/menetrend.css")
    </head>

    <body>
        <header>
            <img id="header-logo" src="~/Content/img/routes-icon.png" />
            <h1>Menetrendkereső Szolgáltatás</h1>
            <nav>
                <ul>
                    <li class="@(ViewContext.RouteData.Values["Controller"].ToString() == "RoutePlanner" ? "selected" : "")">
                        @Html.ActionLink("Útvonaltervezés", "Index", "RoutePlanner")
                    </li>
                    <!--
                    <li class="@(ViewContext.RouteData.Values["Controller"].ToString() == "DatabaseBrowser" ? "selected" : "")">
                        @Html.ActionLink("Menetrend Böngészése", "Index", "DatabaseBrowser")
                        -->
                    </li>
                    <li class="@(ViewContext.RouteData.Values["Controller"].ToString() == "Admin" ? "selected" : "")">
                        @Html.ActionLink("Adminisztráció", "Index", "Admin")
                    </li>
                </ul>
            </nav>
        </header>
        
        <div id="content">
            <div id="main">
                @RenderBody()
            </div>
            
            <aside>
                @RenderSection("sidebar", required: false)
            </aside>
        </div>

        @Scripts.Render("~/js/jquery.js")

        @RenderSection("before_scripts", required: false)

        @Scripts.Render("~/js/menetrend.js")

        @RenderSection("scripts", required: false)
    </body>
</html>